# 正式リリース・実運用投入判定 2026-08-02

| 項目 | 値 |
| --- | --- |
| 判定日 | 2026-08-02 |
| 基準 main SHA | `5b5eb0adba406235d8dfe3663fd26599f159b14a` |
| ライセンス | MIT License（[decisions/0001-mit-license.md](../decisions/0001-mit-license.md)） |
| open `release-blocker` | 0 件 |
| open Issue | 0 件 |
| open PR | 0 件 |
| main の CI | 成功（4 ジョブ: 静的検証・マイグレーションと統合テスト・画面テスト・コンテナのビルド） |
| `pnpm verify` | 成功（87 ファイル / 1314 件、E2E 18 件） |
| 依存の脆弱性（`pnpm check:audit`） | moderate 以上 0 件、見送り例外 0 件 |
| マイグレーション | 空のデータベースへの全適用、二度目の適用、`db:verify` すべて成功 |
| コンテナ | `docker/api.Dockerfile` のビルド成功。非 root（`node`）、開発用依存を含まない |
| セキュリティのクローズアウト | P16-SR1 と P16-SR2 を完了 |
| 判定 | **満たす** |

## 判定の意味

この判定は、基準 main SHA に対し、StaffWeave を正式なリリース候補として扱い、
実データを用いたセルフホスト運用の評価を開始できる状態であると判断した記録です。

次のいずれも保証しません。

- すべての国と地域の労働法令への適合
- あらゆる攻撃に対する安全性
- 可用性、または障害時にデータが失われないこと
- マネージド提供が可能であること

GitHub リポジトリの公開・非公開とは別の判定です。リポジトリはすでに公開されています。

## セキュリティのクローズアウト

公開前のセキュリティ確認（P16-SR1）で 9 件、その独立再検証で 2 件を起票し、
すべて解消しました。

| Issue | 範囲 |
| --- | --- |
| #101 | セッションの絶対有効期限 |
| #102 | 端末登録トークンの有効期限 |
| #103 | Agent 資格情報ファイルの権限とリンクの扱い |
| #104 | Agent の接続先の暗号化 |
| #105 | connector の接続先の暗号化 |
| #106 | Webhook 送信先の暗号化 |
| #107 | 本人によるパスワードの変更 |
| #108 | バックアップの権限 |
| #109 | CLI の秘密情報の入力元 |
| #119 | Agent と connector の未検査リダイレクト（PR #121） |
| #120 | Agent 資格情報の保存先ディレクトリ（PR #122） |

#119 と #120 は、#101〜#109 を実装者以外の視点で再検証（P16-SR1）した際に見つけたものです。
どちらも修正後に、実装を変更しない状態で再確認（P16-SR2）しています。

## この記録の扱い

ここに書いた件数は、判定した時点で数えたものです。
現在の Issue と PR の状態は GitHub を正本とし、正式リリースのたびに数え直します。
この文書が自動で追随することはありません。

## 残っている作業

正式リリースのタグ、成果物、リリースノートの方針は、この判定には含めていません。
`docs/roadmap.md` の P16 には、セッション一覧と一括失効、SBOM が未着手として残っています。
どちらもこの判定を妨げる条件ではありませんが、正式なタグを打つ前に扱うかどうかは
別の判断とします。
