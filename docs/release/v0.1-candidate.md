# v0.1 の候補と、その凍結

いま外部の確認へ回している commit を、1 つだけ書きます。

外部の確認は数週間かかります。その間に main が動き続けると、
「何を確かめたのか」が誰にも言えなくなります。だから候補を止めます。

## 1. いまの候補

```text
96770bed4b5d06f591f9cf1238296b2453e3033b
```

| 項目 | 結果 |
| --- | --- |
| `pnpm verify` | 0（`verify:upgrade` と `verify:restore` は CI で確認） |
| `pnpm verify:release-candidate` | 0（同上） |
| `pnpm release:assets` / `pnpm release:verify` | 0 |
| Agent の配布物を実際に起動 | 0 |
| CI | [31144283339](https://github.com/toshtag/StaffWeave/actions/runs/31144283339) |
| 実行時の検証 | [31144283388](https://github.com/toshtag/StaffWeave/actions/runs/31144283388) |
| SBOM | [31144283395](https://github.com/toshtag/StaffWeave/actions/runs/31144283395) |

判定の中身は [checklist.md](checklist.md) の 1 章と 2 章にあります。

`verify:upgrade` と `verify:restore` は `psql` を要ります。手元に無い環境では走らないため、
CI の「実行時の検証」を根拠にします。手元だけで 0 を確かめたことにはしません。

## 2. 前の候補との違い

前の候補（`6edb299`）は、v0.1 の必須機能を満たしていましたが、
`docs/product/capability-matrix.md` に `partial` 14 件・`planned` 18 件が残っていました。
「候補になった」ことと「当初想定した製品像が完成した」ことは同じではありません。

この候補では、その差を埋めています（[roadmap.md](../roadmap.md) の P27）。

- 承認しきった残業・休日出勤・打刻修正を、日次の計算へ効かせる
- 週・清算期間・変形労働の対象期間の集計
- 休暇の付与規則と、自動付与・一斉付与・CSV 取込・管理簿・失効予定
- 申請まわりの通知（正本は DB）
- 勤務区分と申請種別の CSV 一括投入
- 長時間労働の警告
- IC カードの読み取り（PC/SC）と、打刻時の位置情報（組織ごとの opt-in）
- 正式版を配る仕組み（版の正本・成果物・checksum・対応の検証・release workflow）

残っている `partial` と `planned` は
[capability-matrix.md](../product/capability-matrix.md) が正本です。
いずれも、実機で確かめていないものか、v0.1 の範囲外として次期へ回したものです。

## 3. 凍結の範囲

候補を止めている間、main へ入れてよいのは次だけです。

- 外部の確認で見つかった、重大・高リスクの修正
- リリースを妨げる不具合の修正
- 文書の誤りの訂正

**新しい機能は入れません。**

入れたくなったら、それは v0.1 に要るものだったということです。
[product/v0.1-scope.md](../product/v0.1-scope.md) の判断が間違っていたことになるので、
機能を足す前に、その文書を直してください。

## 4. 外部の確認

4 つを並行して進めます。手順と記録欄は [checklist.md](checklist.md) の 4 章です。
実施者が証拠を記録する場所として、それぞれ Issue を立ててあります。

| 確認 | 状態 | Issue |
| --- | --- | --- |
| 実機の打刻端末（IC カード） | 未実施 | [#194](https://github.com/toshtag/StaffWeave/issues/194) |
| 実機の打刻端末（Windows サービス） | 未実施 | [#195](https://github.com/toshtag/StaffWeave/issues/195) |
| 第三者によるセキュリティ確認 | 未実施 | [#196](https://github.com/toshtag/StaffWeave/issues/196) |
| 試行運用 | 未実施 | [#197](https://github.com/toshtag/StaffWeave/issues/197) |

見つかったものは、その場で直さずに Issue へ貯めます。
1 件ごとに直すと、そのたびに候補が変わり、確認をやり直すことになります。

## 5. 候補が変わるとき

重大・高リスクの修正を入れると、候補の commit は別のものになります。

そのとき、古い候補で得た確認の結果を、そのまま新しい候補の結果として使えません。
何を直したかによって、やり直す範囲が変わります。

| 直したもの | やり直すもの |
| --- | --- |
| 端末・Agent・登録 | 実機の確認 |
| 認証・認可・秘密情報・外部へ出る経路 | 第三者の確認 |
| 打刻・申請・承認・締め・給与への引き渡し | 試行運用のうち、その経路 |
| 上記に触れない文書だけ | なし |

やり直す範囲を決めたら、この文書の 1 章と 4 章を新しい候補で書き直します。

## 6. 正式リリースの判断

4 つの確認がすべて済み、重大・高リスクの Issue が 0 になったら、
最後の候補で次を実行します。

```sh
pnpm verify:release-candidate
pnpm release:assets
pnpm release:verify
```

そのうえで `v0.1.0` の tag を押すと、`.github/workflows/release.yml` が
同じものを組み直し、元との対応を確かめてから GitHub Release を作ります。

揃えるものは [checklist.md](checklist.md) の 6 章にあります。

署名は行いません。署名には所有者の鍵が要り、鍵を持たない者が付けた署名は
「誰が配ったか」を示せません。
