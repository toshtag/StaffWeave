# v0.1 の候補と、その凍結

いま外部の確認へ回している commit を、1 つだけ書きます。

外部の確認は数週間かかります。その間に main が動き続けると、
「何を確かめたのか」が誰にも言えなくなります。だから候補を止めます。

## 1. いまの候補

```text
ac01ecd6d9af5bb1ca231c2f7f1dc7632a27bb31
```

| 項目 | 結果 |
| --- | --- |
| `pnpm verify` | 0（`verify:upgrade` と `verify:restore` は psql が要るため CI で確認） |
| `pnpm verify:release-candidate` | 0（同上） |
| CI | [31194380490](https://github.com/toshtag/StaffWeave/actions/runs/31194380490) |
| 実行時の検証 | [31194380504](https://github.com/toshtag/StaffWeave/actions/runs/31194380504) |
| SBOM | [31194379431](https://github.com/toshtag/StaffWeave/actions/runs/31194379431) |
| Windows の配布物 | [31194379742](https://github.com/toshtag/StaffWeave/actions/runs/31194379742) |
| Release の試走（tag を作らない） | [31194807896](https://github.com/toshtag/StaffWeave/actions/runs/31194807896) |

tag はまだ作りません。外部の確認（#194〜#197）が残っているためです。

### この候補で足したもの

前の候補（`857c820`）との差は #260〜#262 として直しました。いずれも
Windows の実機経路を止めていたものです。

**常駐の方法を変えました。** `sc.exe create` で `node.exe` をサービスとして登録する
形は、登録は通っても起動できません。サービスとして動くプロセスは Service Control
Manager と話す入口を持っている必要があり、`node.exe` も `cli.js` も持っていません。
タスクスケジューラで起動時に始める形へ変え、決定として残しました
（[decisions/0002-windows-residency.md](../decisions/0002-windows-residency.md)）。

**読み取り装置の部品を、端末で取り寄せる形をやめました。** 配ったあとに
`npm install` する形だと、配布物だけでは物理カードを読めず、確かめた構成と実際に
動く構成も別になります。Windows の上で組むときに配布物へ入れ、通信を切った端末でも
そのまま使えるようにしました。

**止められるようにしました。** カードを待っている間の打ち切りができず、止めろと
言われても読み取り側が戻りませんでした。PC/SC の待ち受けも外していなかったため、
長く動かすほど溜まりました。

いずれも、Windows の上で実際に動かして確かめています（「Windows の配布物」）。
文字列を読むだけの検査では、同じ間違いをもう一度見逃します。

### 取り下げた候補

```text
857c820d95ca6c911e65ba41239827000e7b8f47
f0a4e1436475803576f525f4f61d279ef89304ab
```

`857c820` は、Windows の実機経路にコードから確定できる阻害要因が残っていたため
取り下げました（#260〜#262）。CI はすべて緑でしたが、Windows の上で動かす検査が
無かったため、そこが見えていませんでした。

`f0a4e14` は、追補のレビューで 4 件が見つかったため取り下げました（#250〜#253）。
いちばん大きいのは物理カードの打刻が回線断で失われ得たことです。

### さらに前に取り下げた候補

```text
96770bed4b5d06f591f9cf1238296b2453e3033b
```

| 項目 | 結果 |
| --- | --- |
| `pnpm verify` | 0（`verify:upgrade` と `verify:restore` は CI で確認） |
| `pnpm verify:release-candidate` | 0（同上） |
| `pnpm release:assets` / `pnpm release:verify` | 0 |
| Agent の配布物を実際に起動 | 0 |
| CI | [31144283339](https://github.com/toshtag/StaffWeave/actions/runs/31144283339) |
| 実行時の検証 | [31144283388](https://github.com/toshtag/StaffWeave/actions/runs/31144283388) |
| SBOM | [31144283395](https://github.com/toshtag/StaffWeave/actions/runs/31144283395) |

これらがすべて 0 でも、候補としては足りませんでした。
`pnpm verify` が見るのは「直したものが壊れていないか」であって、
「設定した値が実行時の経路で効いているか」ではありません。
その差は縦に通す検査でしか見つかりません。

判定の中身は [checklist.md](checklist.md) の 1 章と 2 章にあります。

`verify:upgrade` と `verify:restore` は `psql` を要ります。手元に無い環境では走らないため、
CI の「実行時の検証」を根拠にします。手元だけで 0 を確かめたことにはしません。

## 2. 前の候補との違い

最初の候補（`6edb299`）は、v0.1 の必須機能を満たしていましたが、
`docs/product/capability-matrix.md` に `partial` 14 件・`planned` 18 件が残っていました。
（いまの一覧は `implemented` 105 件・`partial` 17 件・`planned` 9 件・`non-goal` 5 件です。
`partial` が増えたのは能力が減ったからではなく、縦に通る証拠の無いものを
`implemented` と書くのをやめたためです。）
「候補になった」ことと「当初想定した製品像が完成した」ことは同じではありません。

取り下げた候補では、その差を埋めたつもりでいました（[roadmap.md](../roadmap.md) の P27）。
実際には、次のうちいくつかは API としては存在しても、利用者が使う経路から
計算や保存まで届いていませんでした。

- 承認しきった残業・休日出勤・打刻修正を、日次の計算へ効かせる
- 週・清算期間・変形労働の対象期間の集計
- 休暇の付与規則と、自動付与・一斉付与・CSV 取込・管理簿・失効予定
- 申請まわりの通知（正本は DB）
- 勤務区分と申請種別の CSV 一括投入
- 長時間労働の警告
- IC カードの読み取り（PC/SC）と、打刻時の位置情報（組織ごとの opt-in）
- 正式版を配る仕組み（版の正本・成果物・checksum・対応の検証・release workflow）

残っている `partial` と `planned` は
[capability-matrix.md](../product/capability-matrix.md) が正本です。
いずれも、実機で確かめていないものか、v0.1 の範囲外として次期へ回したものです。

## 3. 凍結の範囲

候補を止めている間、main へ入れてよいのは次だけです。

- 外部の確認で見つかった、重大・高リスクの修正
- リリースを妨げる不具合の修正
- 文書の誤りの訂正

**新しい機能は入れません。**

入れたくなったら、それは v0.1 に要るものだったということです。
[product/v0.1-scope.md](../product/v0.1-scope.md) の判断が間違っていたことになるので、
機能を足す前に、その文書を直してください。

## 4. 外部の確認

4 つを並行して進めます。手順と記録欄は [checklist.md](checklist.md) の 4 章です。
実施者が証拠を記録する場所として、それぞれ Issue を立ててあります。

| 確認 | 状態 | Issue |
| --- | --- | --- |
| 実機の打刻端末（IC カード） | 未実施 | [#194](https://github.com/toshtag/StaffWeave/issues/194) |
| 実機の打刻端末（Windows サービス） | 未実施 | [#195](https://github.com/toshtag/StaffWeave/issues/195) |
| 第三者によるセキュリティ確認 | 未実施 | [#196](https://github.com/toshtag/StaffWeave/issues/196) |
| 試行運用 | 未実施 | [#197](https://github.com/toshtag/StaffWeave/issues/197) |

見つかったものは、その場で直さずに Issue へ貯めます。
1 件ごとに直すと、そのたびに候補が変わり、確認をやり直すことになります。

## 5. 候補が変わるとき

重大・高リスクの修正を入れると、候補の commit は別のものになります。

そのとき、古い候補で得た確認の結果を、そのまま新しい候補の結果として使えません。
何を直したかによって、やり直す範囲が変わります。

| 直したもの | やり直すもの |
| --- | --- |
| 端末・Agent・登録 | 実機の確認 |
| 認証・認可・秘密情報・外部へ出る経路 | 第三者の確認 |
| 打刻・申請・承認・締め・給与への引き渡し | 試行運用のうち、その経路 |
| 上記に触れない文書だけ | なし |

やり直す範囲を決めたら、この文書の 1 章と 4 章を新しい候補で書き直します。

## 6. 正式リリースの判断

4 つの確認がすべて済み、重大・高リスクの Issue が 0 になったら、
最後の候補で次を実行します。

```sh
pnpm verify:release-candidate
pnpm release:assets
pnpm release:verify
```

そのうえで `v0.1.0` の tag を押すと、`.github/workflows/release.yml` が
その commit を確かめてから、同じものを組み直し、元との対応を確かめて
GitHub Release を作ります。

確かめるのは 2 つです。tag の名前と package の版が一致していること、
そして **その SHA で** 必須の workflow（CI・runtime・SBOM）が成功していること。
どちらかを満たさなければ、成果物を組むところまで進みません。

branch の最新が緑であることは、tag を押した commit が緑であることを
意味しません。古い成功は流用できません。

「実行時の検証」は対象を絞っているため、文書だけを直した commit では走りません。
候補として凍結したい commit がそれに当たる場合は、Actions からその ref を指定して
手で走らせ、その SHA の成功を作ってください。走らせずに通す道はありません。

揃えるものは [checklist.md](checklist.md) の 6 章にあります。

署名は行いません。署名には所有者の鍵が要り、鍵を持たない者が付けた署名は
「誰が配ったか」を示せません。
