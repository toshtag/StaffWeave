# 変更の記録

版ごとに、何が変わったかを書きます。日付は tag を付けた日です。

版は `package.json` の `version` が正本です。ここに書く版は、その値と揃えます。

## v0.1.0（未リリース）

最初の正式版です。リリース候補から正式版へ進めるにあたり、
「候補になった」ことと「当初想定した製品像が完成した」ことの差を埋めました。

### 勤怠の正しさ

- 承認しきった残業・休日出勤・打刻修正の申請を、日次の計算へ効かせるようにしました。
  残業は申請した上限時刻までを認定し、超えた分を分けて出します。
  締め済みの期間を含む申請は、承認そのものを断ります。
- 週・フレックスの清算期間・変形労働の対象期間の集計と、総枠との差を出せるようにしました。
- 休暇の付与規則（勤続の段）と、それに従った自動付与・一斉付与・CSV 取込を作りました。
  同じ日への二重付与は DB の制約が止めます。
- 休暇管理簿（期首・付与・消化・失効・期末）と、失効の予定を出せるようにしました。

### 実運用 UX

- 申請・承認・差し戻し・取消・代理承認の通知を、製品内で届けるようにしました。
  通知の正本は DB に残ります。
- 勤務区分と申請種別を CSV でまとめて入れられるようにしました。
  1 行でも読めなければ 1 行も取り込みません。
- 長時間労働の警告を出せるようにしました。上限は計算規則の版が持ち、
  置かないかぎり警告を出しません。

### 配布

- 版を `package.json` の 1 か所で決めるようにしました。
- 端末の配布物・構成一覧・checksum をまとめて出す手順（`pnpm release:assets`）と、
  それらが元と噛み合っているかを確かめる手順（`pnpm release:verify`）を足しました。
- tag から GitHub Release を作るワークフローを足しました。

### まだ無いもの

`docs/product/capability-matrix.md` の `planned` と、
`docs/product/v0.1-scope.md` の「次期へ回すもの」がそのまま残りの一覧です。
