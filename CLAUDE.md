# このリポジトリで AI エージェントに作業させるときの指示

Claude Code や Codex などに変更を任せるときの決めごとです。
寄稿者へ課す規則ではありません。人が守ることは [CONTRIBUTING.md](CONTRIBUTING.md) に、
コードの正しさに関わる制約は
[docs/development/architecture.md](docs/development/architecture.md) にあります。

## 書かないもの

- 生成ツール由来の定型文。`Co-authored-by` の署名や「Generated with ...」の類を、
  commit・PR・branch 名・ファイルの中身のどこにも入れない。
- 他の製品を引き合いに出した説明。機能の要件は StaffWeave の設計として書く。
  引き合いに出すと相手の名前や画面の写しが入り込み、相手の版が変われば
  説明だけが古くなる。
- 止めたい固有名そのもの。符号化しても元へ戻せるため、リポジトリへ書かない。

## 追跡しないもの

- 画像と PDF。差分で中身を読めないため、何が写っているかを確かめないまま
  再配布することになる。図は本文か SVG で書く。
- 進捗ファイル・計画ファイル・タスクファイル。課題と完了条件は GitHub Issue、
  実装差分と検証結果は PR、機械的な合否は CI が正本。
  同じ完了状態を複数の場所へ置くと、必ずどこかが古くなる。

## 文章

- 1 文を短くする。小さな判断ごとに長い因果説明を付けない。
- 後世に要る「なぜ」は ADR（`docs/decisions/`）へ。破るとバグになるものはテストへ。
  どちらでもないものを prose で積み上げない。
- 同じ事実を複数の文書で維持しない。
- 実装の詳細・commit SHA・Issue 番号を、利用者向けのガイドへ書かない。

## 変更の進め方

- 受け入れ条件を先に短く決めてから実装する。
- 文書を先に大量に作らない。実装に要る最小限を、同じ変更で更新する。
- 使われない抽象化、空のパッケージ、将来用だけのコードを作らない。
- 一時的な TODO には、理由と解消条件を書く。
- commit は 1 つのことだけを含め、なぜそうしたかを本文へ書く。
